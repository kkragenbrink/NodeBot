plugins:
  - meetme

log_level                   : debug

contexts:
  Mud:
    client:
      name                  : 'SandBox'
      hostname              : 'writh.net'
      port                  : 1619
      username              : 'NodeBot'
      password              : '!'
      type                  : mush
    input:
      prefix                : '\+'
    output:
      header                : '[center(< %s >, 78, =)]'
      mid                   : '[repeat(-,78)]'
      prefix                : '\[%s\]'
      tail                  : '[repeat(=, 78)]'
