plugins                     : [Jobs]
log_level                   : debug

contexts:
  Mud:
    client:
      name                  : 'NodeBox'
      hostname              : 'localhost'
      port                  : 2067
      username              : 'NodeBot'
      password              : '!'
      type                  : mush
    input:
      prefix                : '\+'
    output:
      header                : '[center(< %s >, 78, =)]'
      mid                   : '[repeat(-,78)]'
      prefix                : '\[%s\]'
      tail                  : '[repeat(=, 78)]'